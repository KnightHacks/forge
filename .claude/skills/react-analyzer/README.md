# react-analyzer skill

Repo-level skill wrapper for `azer/react-analyzer`.

The actual analyzer dependency is installed as a dev dependency and exposed via:

```bash
pnpm analyze:react <files-or-directories>
```
