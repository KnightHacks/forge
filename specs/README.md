# Historical specifications

These reverse-engineering specifications record evidence used while rebuilding Forge. Any cited path under the removed `legacy/` tree is preserved in Git history rather than the current workspace.
